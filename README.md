### backend for dvij

#### how to start

  * `go get`
  * `go get -v ./...`
  * `go run main.go start std`
  * `http://localhost:8080`

# Vue 2.0 dvij.front

## Build Setup

#### install dependencies
`npm install`

#### start on localhost:8081
`npm run start`

#### build production
`npm run build`
